import type { Metadata } from "next";
export const metadata: Metadata = { title: "Magento to Shopify Migration — Shoman Solutions" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
