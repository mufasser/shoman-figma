import type { Metadata } from "next";
export const metadata: Metadata = { title: "White-Label Agency Partnerships — Shoman Solutions" };
export default function Layout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
